import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { API_URL } from '../config/api-url.config'; import { AreaOrganizativa } from '../interfaces/area-organizativa.interface';
@Injectable({providedIn:'root'}) export class AreaOrganizativaService { private url=`${API_URL}/areas-organizativas`; constructor(private http:HttpClient){} consultar(){return this.http.get<AreaOrganizativa[]>(this.url)} guardar(v:AreaOrganizativa){return this.http.post<AreaOrganizativa>(this.url,v)} actualizar(v:AreaOrganizativa){return this.http.put<AreaOrganizativa>(`${this.url}/${v.areId}`,v)} eliminar(id:number){return this.http.delete(`${this.url}/${id}`)} }
