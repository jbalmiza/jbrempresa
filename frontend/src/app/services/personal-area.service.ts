import { Injectable } from '@angular/core'; import { HttpClient } from '@angular/common/http'; import { API_URL } from '../config/api-url.config'; import { PersonalArea } from '../interfaces/personal-area.interface';
@Injectable({providedIn:'root'}) export class PersonalAreaService { private url=`${API_URL}/personal-area`; constructor(private http:HttpClient){} consultar(){return this.http.get<PersonalArea[]>(this.url)} guardar(v:PersonalArea){return this.http.post<PersonalArea>(this.url,v)} actualizar(v:PersonalArea){return this.http.put<PersonalArea>(`${this.url}/${v.peaId}`,v)} eliminar(id:number){return this.http.delete(`${this.url}/${id}`)} }
